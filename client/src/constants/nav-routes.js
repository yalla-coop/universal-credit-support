const GENERAL = {
  HOME: '/',
};

const stepsBase = '/steps';
const STEPS = {
  STEP1: `${stepsBase}/1`,
};

const EXTERNAL = {
  TERMS_OF_USE: 'www.google.com/',
  PRIVACY_POLICY: 'www.google.com/',
};

export { GENERAL, STEPS, EXTERNAL };
